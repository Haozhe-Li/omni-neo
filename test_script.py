from core.subagents.researcher import researcher
from deepagents import create_deep_agent
import asyncio


async def test():
    # Because researcher is a dict, you might need to create an agent out of it
    # But let's check deepagents usage. create_deep_agent is used in supervisor.
    pass
