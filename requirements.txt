# For building agents
# Pinned to a minor line: core/agent.py registers a HarnessProfile, and
# `deepagents.profiles` is a documented beta API that may change shape.
deepagents>=0.6.12,<0.7
langchain
langchain_community
langgraph
python-dotenv
langchain-groq
langchain-openai
langchain-google-genai
langchain-cerebras

# For persisting agent state — LangGraph checkpoints in Upstash Redis (HTTP REST),
# app data in Supabase over PostgREST (HTTP). No direct Postgres/TCP anymore.
langgraph-checkpoint-upstash-redis
supabase

# For fetching data
spider-client
tavily-python
yfinance
pyowm
arxiv

# For running agents
uvicorn
fastapi
groq
python-multipart

# For authentication (Clerk JWT verification)
PyJWT[crypto]
cryptography

# For coding sandbox
e2b-code-interpreter
numpy
pandas
matplotlib
boto3

# cache + stream buffer + checkpoints (Upstash Redis over HTTP REST)
upstash-redis

# RAG
firecrawl-anydoc
langchain-text-splitters

# semantic source search (backs /check_source)
upstash-search

# scheduled research tasks: email delivery + cron scheduling
resend
qstash